load("@rules_rust//rust:defs.bzl", "rust_library")

rust_library(
    name = "daemon-utils",
    srcs = glob(["src/**/*.rs"]),
    crate_name = "daemon_utils",
    tags = ["rust"],
    visibility = ["//visibility:public"],
    deps = [
        "//libs/logging:rust",
    ],
)
