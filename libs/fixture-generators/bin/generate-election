#!/usr/bin/env node

require('tsx/cjs');

process.exitCode = require('../src/cli/generate-election').main(process.argv, {
  stdin: process.stdin,
  stdout: process.stdout,
  stderr: process.stderr,
});
