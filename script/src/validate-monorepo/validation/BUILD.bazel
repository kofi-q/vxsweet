load("//tools/ts_build:defs.bzl", "ts_package")

ts_package(
    name = "validation",
    src_deps = [
        "//:node_modules/@types/node",
        "//:node_modules/matcher",
        "//:node_modules/typescript",
        "//libs/monorepo-utils/src",
    ],
)
